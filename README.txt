This is a very small application that monitors your Internet connection to ensure you actually can access the outside world.

To install, simply drag the Network Monitor icon into the Applications folder.

To run, just double click the Network Monitor app. The application presents itself as an icon in your menu bar.